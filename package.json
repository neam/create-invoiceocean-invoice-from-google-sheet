{
  "name": "invoiceocean-invoice-importer",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format": "prettier './**/*.{css,ts,js,json,md}' --trailing-comma=all --ignore-path=.prettierignore --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "argparse": "^1.0.10"
  },
  "devDependencies": {
    "dotenv": "^6.0.0",
    "prettier": "^1.14.3"
  }
}
